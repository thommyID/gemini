google_accounts.json
installation_id
state.json
trustedFolders.json

# Ignore noisy/temporary tool outputs but keep chat logs
**/tool-outputs/
**/background-processes/
**/bin/

# Keep git config local if needed, but normally we track the repo config
.gitconfig
.gitconfig_system_empty